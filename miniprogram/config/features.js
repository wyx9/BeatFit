// ===== 功能开关 =====
// 上线前临时屏蔽非核心功能
// 恢复时只需将对应值改为 true

module.exports = {
  ENABLE_LEADERBOARD: false,       // 排行榜页面 + 底部导航入口
  ENABLE_WORKOUT_RECORDS: false,   // 我的 → 训练记录
  ENABLE_ACHIEVEMENTS: false,      // 我的 → 成就徽章
  ENABLE_ROOM_SETTINGS: false      // 创建房间 → 房间设置
}
